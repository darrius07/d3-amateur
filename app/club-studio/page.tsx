import Image from 'next/image';import {redirect} from 'next/navigation';import {createClient} from '@/lib/supabase/server';import {createAdminClient} from '@/lib/supabase/admin';import {deleteClubLogo,uploadClubLogo} from './actions';
export default async function ClubStudio(){const auth=await createClient();const {data:{user}}=await auth.auth.getUser();if(!user)redirect('/login?returnTo=/club-studio');const admin=createAdminClient();const {data:memberships}=await admin.from('club_memberships').select('club_id,role').eq('user_id',user.id).eq('active',true).eq('role','OWNER');if(!memberships?.length)redirect('/my/claims');const ids=memberships.map(m=>m.club_id);const {data:clubs}=await admin.from('clubs').select('id,display_name,city,claim_status,logo_path,slug').in('id',ids);const signed=new Map<string,string>();for(const club of clubs??[]){if(club.logo_path){const {data}=await admin.storage.from('club-assets').createSignedUrl(club.logo_path,3600);if(data)signed.set(club.id,data.signedUrl)}}return <main className="main studio"><p className="eyebrow">Club Studio</p><h1>Vos clubs</h1>{clubs?.map(club=><section className="studio-card" key={club.id}><div>{signed.get(club.id)?<Image unoptimized width={120} height={120} className="studio-logo" src={signed.get(club.id)!} alt={`Logo ${club.display_name}`}/>:<div className="club-logo">D3</div>}</div><div><span className="verified">✓ Club vérifié D3</span><h2>{club.display_name}</h2><p>{club.city} · Rôle OWNER</p><form action={uploadClubLogo}><input type="hidden" name="club_id" value={club.id}/><label className="upload-label">{club.logo_path?'Modifier le logo':'Ajouter un logo'}<input name="logo" type="file" accept="image/png,image/jpeg,image/webp" required/></label><small>PNG, JPEG ou WebP · 5 Mo maximum</small><button className="button">Enregistrer le logo</button></form>{club.logo_path&&<form action={deleteClubLogo}><input type="hidden" name="club_id" value={club.id}/><button className="text-danger">Supprimer le logo</button></form>}</div><div className="future-modules"><span>Équipes · bientôt</span><span>Effectif · bientôt</span><span>Matchs · bientôt</span><span>Sponsors · bientôt</span></div></section>)}</main>}
