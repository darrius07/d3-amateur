import {login,signup} from './actions';
export default async function LoginPage({searchParams}:{searchParams:Promise<{message?:string;returnTo?:string}>}){const {message,returnTo='/club-studio'}=await searchParams;return <main className="main auth-grid"><section className="card"><p className="eyebrow">Espace sécurisé</p><h1>Connexion</h1><form action={login}><input type="hidden" name="returnTo" value={returnTo}/><label htmlFor="email">Adresse e-mail</label><input className="field" id="email" name="email" type="email" autoComplete="email" required/><label htmlFor="password">Mot de passe</label><input className="field" id="password" name="password" type="password" autoComplete="current-password" required/><button className="button">Se connecter</button></form></section><section className="card"><p className="eyebrow">Nouveau sur D3</p><h2>Créer un compte</h2><form action={signup}><input type="hidden" name="returnTo" value={returnTo}/><label htmlFor="display_name">Nom affiché</label><input className="field" id="display_name" name="display_name" required/><label htmlFor="signup_email">Adresse e-mail</label><input className="field" id="signup_email" name="email" type="email" required/><label htmlFor="signup_password">Mot de passe</label><input className="field" id="signup_password" name="password" type="password" minLength={8} required/><button className="button">Créer mon compte</button></form></section>{message&&<p className="message" role="status">{message}</p>}</main>}
