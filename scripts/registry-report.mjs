import nextEnv from '@next/env';import {createClient} from '@supabase/supabase-js';nextEnv.loadEnvConfig(process.cwd());const db=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL,process.env.SUPABASE_SERVICE_ROLE_KEY,{auth:{persistSession:false}});for(const [label,table,filter] of [['staging','staging_rna_associations'],['candidates','staging_rna_associations',['processing_status','matched']],['needs_review','staging_rna_associations',['processing_status','needs_review']],['clubs','clubs'],['rna_links','external_identities',['provider','RNA']]]){let q=db.from(table).select('*',{count:'exact',head:true});if(filter)q=q.eq(filter[0],filter[1]);const {count,error}=await q;if(error)throw error;console.log(`${label}=${count}`)}const {data,error}=await db.from('clubs').select('display_name,city,slug').order('created_at',{ascending:false}).limit(5);if(error)throw error;console.log(JSON.stringify(data));
